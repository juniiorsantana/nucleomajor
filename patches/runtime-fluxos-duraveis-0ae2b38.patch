diff --git a/whatsapp-assistant/chatbot_runtime.py b/whatsapp-assistant/chatbot_runtime.py
index b2c2e39..b697d4d 100644
--- a/whatsapp-assistant/chatbot_runtime.py
+++ b/whatsapp-assistant/chatbot_runtime.py
@@ -9,6 +9,8 @@ from __future__ import annotations
 
 from dataclasses import dataclass
 from datetime import datetime, timezone
+import threading
+import time
 from typing import Any, Optional
 
 import audit
@@ -18,6 +20,7 @@ from operator_verification import OperatorVerificationError, OperatorVerifier
 
 
 IDENTITY_EVENTS = {"contact.created", "contact.imported", "contact.whatsapp_linked"}
+MAX_DURABLE_STEPS = 200
 
 
 @dataclass(frozen=True)
@@ -72,13 +75,39 @@ def _condition_matches(condition: dict, contact: dict, now: float) -> bool:
     return False
 
 
+def _expression_matches(expression: Any, contact: dict, now: float) -> bool:
+    if isinstance(expression, list):
+        return bool(expression) and all(
+            isinstance(item, dict) and _expression_matches(item, contact, now)
+            for item in expression
+        )
+    if not isinstance(expression, dict):
+        return False
+    operator = expression.get("operador")
+    if operator in {"e", "ou"}:
+        items = expression.get("itens")
+        if not isinstance(items, list) or not items:
+            return False
+        # Definição malformada nunca pode virar um match por descarte dos
+        # itens inválidos (especialmente no operador "e").
+        matches = (_expression_matches(item, contact, now) for item in items)
+        return all(matches) if operator == "e" else any(matches)
+    return _condition_matches(expression, contact, now)
+
+
 def _bot_matches(bot: dict, contact: dict, now: float) -> bool:
     definition = bot.get("definition") if isinstance(bot.get("definition"), dict) else {}
     conditions = definition.get("condicoes")
-    return bool(conditions) and all(
-        isinstance(item, dict) and _condition_matches(item, contact, now)
-        for item in conditions
-    )
+    return _expression_matches(conditions, contact, now)
+
+
+def _canvas_version(bot: dict) -> int:
+    definition = bot.get("definition") if isinstance(bot.get("definition"), dict) else {}
+    canvas = definition.get("canvas") if isinstance(definition.get("canvas"), dict) else {}
+    try:
+        return int(canvas.get("versao") or 1)
+    except (TypeError, ValueError):
+        return 1
 
 
 def _path(definition: dict) -> list[dict]:
@@ -170,6 +199,55 @@ class ChatbotExecutor:
         self.arbitro = arbitro
         self.api_base_url = api_base_url
         self.api_token = api_token
+        self._stop = threading.Event()
+        self._thread: Optional[threading.Thread] = None
+
+    def start(self) -> None:
+        if self._thread and self._thread.is_alive():
+            return
+        self._stop.clear()
+        self._thread = threading.Thread(
+            target=self._poll_loop,
+            name="chatbot-flow-poller",
+            daemon=True,
+        )
+        self._thread.start()
+
+    def stop(self) -> None:
+        self._stop.set()
+        if self._thread:
+            self._thread.join(timeout=5)
+
+    def _poll_loop(self) -> None:
+        while not self._stop.is_set():
+            try:
+                self.poll_once()
+            except OperatorVerificationError as exc:
+                audit.log("flow.poll_failed", error=str(exc))
+            except Exception as exc:
+                audit.log("flow.poll_failed", error=type(exc).__name__)
+            self._stop.wait(2.0)
+
+    def poll_once(self) -> None:
+        payload = self.verifier.flow_pending()
+        executions = payload.get("executions")
+        if not isinstance(executions, list):
+            raise OperatorVerificationError("fila de fluxos duráveis devolveu formato inválido")
+        for execution in executions:
+            if not isinstance(execution, dict):
+                continue
+            if execution.get("status") == "suspended":
+                expires_at = _timestamp(execution.get("suspensionExpiresAt"))
+                if expires_at is not None and expires_at <= time.time():
+                    self.verifier.flow_finish_ai(
+                        str(execution.get("executionId") or ""),
+                        str(execution.get("suspensionId") or ""),
+                        str(execution.get("requesterPhone") or ""),
+                        "expired",
+                    )
+                continue
+            result = self._run_durable(execution)
+            self._apply_result(execution, result)
 
     def handle(self, payload: dict) -> ChatbotResult:
         sender = str(payload.get("sender_phone") or "").strip()
@@ -192,6 +270,155 @@ class ChatbotExecutor:
         bot = next((item for item in bots if _bot_matches(item, contact, now)), None)
         if bot is None:
             return ChatbotResult(False, DONO_IA, "no_match")
+        if _canvas_version(bot) >= 3:
+            return self._handle_durable(payload, bot)
+        return self._handle_legacy(sender, message_id, connection_id, bot, contact)
+
+    def _handle_durable(self, payload: dict, bot: dict) -> ChatbotResult:
+        sender = str(payload.get("sender_phone") or "").strip()
+        message_id = str(payload.get("message_id") or "").strip()
+        connection_id = str(payload.get("connection_id") or "").strip()
+        session_id = str(payload.get("conversation_session_id") or "").strip()
+        try:
+            epoch = int(payload.get("conversation_epoch") or 0)
+        except (TypeError, ValueError):
+            epoch = -1
+        if not session_id or epoch < 0:
+            return ChatbotResult(False, DONO_HUMANO, "flow_session_invalid")
+        try:
+            execution = self.verifier.flow_start(
+                sender,
+                message_id,
+                str(bot.get("id") or ""),
+                int(bot.get("version") or 1),
+                session_id,
+                epoch,
+            )
+            result = self._run_durable(execution)
+            self._apply_result(execution, result)
+            return result
+        except (OperatorVerificationError, TypeError, ValueError) as exc:
+            audit.log("flow.start_failed", connection_id=connection_id, error=str(exc))
+            return ChatbotResult(True, DONO_HUMANO, "flow_start_failed")
+
+    def _run_durable(self, execution: dict) -> ChatbotResult:
+        current = execution
+        for _ in range(MAX_DURABLE_STEPS):
+            status = str(current.get("status") or "")
+            if status in {"completed", "cancelled"}:
+                return ChatbotResult(True, None, status)
+            if status == "needs_review":
+                return ChatbotResult(True, DONO_HUMANO, "flow_needs_review")
+            if status == "suspended":
+                return ChatbotResult(False, DONO_IA, "flow_waiting_ai")
+            claim = self.verifier.flow_claim(
+                str(current.get("executionId") or ""),
+                int(current.get("revision") or 0),
+            )
+            if claim.get("status") != "executing":
+                current = claim
+                continue
+            step = claim.get("step")
+            contact = claim.get("contact")
+            if not isinstance(step, dict) or not isinstance(contact, dict):
+                self.verifier.flow_cancel(
+                    str(claim.get("executionId") or ""),
+                    "invalid_definition",
+                )
+                return ChatbotResult(True, DONO_HUMANO, "flow_claim_invalid")
+            kind = str(step.get("tipo") or "")
+            output = ""
+            if kind == "enviar_mensagem":
+                message = _render(str(step.get("texto") or ""), contact)
+                try:
+                    send.send_message(
+                        self.api_base_url,
+                        self.api_token,
+                        str(claim.get("requesterPhone") or ""),
+                        message,
+                    )
+                except send.SendError:
+                    self.verifier.flow_ack(
+                        str(claim.get("executionId") or ""),
+                        str(claim.get("claimToken") or ""),
+                        "padrao",
+                        "uncertain",
+                    )
+                    return ChatbotResult(True, DONO_HUMANO, "flow_delivery_unknown")
+                output = "padrao"
+            elif kind == "editar_etiquetas":
+                output = "padrao"
+            elif kind == "condicao":
+                output = (
+                    "sim"
+                    if _expression_matches(
+                        step.get("expressao"),
+                        contact,
+                        datetime.now(timezone.utc).timestamp(),
+                    )
+                    else "nao"
+                )
+            elif kind == "transferir" and step.get("destino") == "ia":
+                current = self.verifier.flow_ack(
+                    str(claim.get("executionId") or ""),
+                    str(claim.get("claimToken") or ""),
+                    "",
+                )
+                return ChatbotResult(False, DONO_IA, "flow_waiting_ai")
+            elif kind == "transferir" and step.get("destino") == "humano":
+                self.verifier.flow_ack(
+                    str(claim.get("executionId") or ""),
+                    str(claim.get("claimToken") or ""),
+                    "",
+                )
+                return ChatbotResult(True, DONO_HUMANO, "flow_handoff")
+            elif kind == "encerrar":
+                self.verifier.flow_ack(
+                    str(claim.get("executionId") or ""),
+                    str(claim.get("claimToken") or ""),
+                    "",
+                )
+                return ChatbotResult(True, None, "flow_closed")
+            else:
+                self.verifier.flow_cancel(
+                    str(claim.get("executionId") or ""),
+                    "invalid_definition",
+                )
+                return ChatbotResult(True, DONO_HUMANO, "flow_step_invalid")
+            current = self.verifier.flow_ack(
+                str(claim.get("executionId") or ""),
+                str(claim.get("claimToken") or ""),
+                output,
+            )
+        self.verifier.flow_cancel(
+            str(current.get("executionId") or ""),
+            "invalid_definition",
+        )
+        return ChatbotResult(True, DONO_HUMANO, "flow_step_limit")
+
+    def _apply_result(self, execution: dict, result: ChatbotResult) -> None:
+        connection_id = str(execution.get("connectionId") or "")
+        requester = str(execution.get("requesterPhone") or "")
+        if not connection_id or not requester:
+            return
+        if result.reason == "flow_closed":
+            self.arbitro.fechar(connection_id, requester, "flow_closed")
+        elif result.next_owner in {DONO_IA, DONO_HUMANO}:
+            self.arbitro.definir_dono(
+                connection_id,
+                requester,
+                result.next_owner,
+                result.reason,
+            )
+
+    def _handle_legacy(
+        self,
+        sender: str,
+        message_id: str,
+        connection_id: str,
+        bot: dict,
+        contact: dict,
+    ) -> ChatbotResult:
         plan = _plan(bot, contact)
         try:
             claim = self.verifier.chatbot_claim(sender, message_id, str(bot.get("id")), int(bot.get("version") or 1))
diff --git a/whatsapp-assistant/operator_verification.py b/whatsapp-assistant/operator_verification.py
index 8053bb8..70351ab 100644
--- a/whatsapp-assistant/operator_verification.py
+++ b/whatsapp-assistant/operator_verification.py
@@ -397,6 +397,98 @@ class OperatorVerifier:
             "conclusão do chatbot",
         )
 
+    def flow_start(
+        self,
+        requester_phone: str,
+        external_message: str,
+        chatbot_id: str,
+        version: int,
+        conversation_session: str,
+        conversation_epoch: int = 0,
+    ) -> dict[str, Any]:
+        return self._rpc(
+            "nucleo_flow_start",
+            {
+                "requester_phone": str(requester_phone or "").strip(),
+                "external_message": str(external_message or "").strip(),
+                "selected_chatbot": str(chatbot_id or "").strip(),
+                "expected_version": int(version),
+                "conversation_session": str(conversation_session or "").strip(),
+                "conversation_epoch": int(conversation_epoch),
+            },
+            "início do fluxo durável",
+        )
+
+    def flow_claim(self, execution_id: str, revision: int) -> dict[str, Any]:
+        return self._rpc(
+            "nucleo_flow_claim",
+            {"execution_id": str(execution_id or "").strip(), "expected_revision": int(revision)},
+            "reserva do fluxo durável",
+        )
+
+    def flow_ack(
+        self,
+        execution_id: str,
+        claim_token: str,
+        selected_output: str,
+        effect_status: str = "confirmed",
+    ) -> dict[str, Any]:
+        return self._rpc(
+            "nucleo_flow_ack",
+            {
+                "execution_id": str(execution_id or "").strip(),
+                "claim_token": str(claim_token or "").strip(),
+                "selected_output": str(selected_output or "").strip(),
+                "effect_status": str(effect_status or "").strip(),
+            },
+            "confirmação do passo durável",
+        )
+
+    def flow_pending(self) -> dict[str, Any]:
+        return self._rpc("nucleo_flow_pending", {}, "fila de fluxos duráveis")
+
+    def flow_current(self, requester_phone: str) -> dict[str, Any]:
+        return self._rpc(
+            "nucleo_flow_current",
+            {"requester_phone": str(requester_phone or "").strip()},
+            "fluxo durável atual",
+        )
+
+    def flow_state(self, execution_id: str) -> dict[str, Any]:
+        return self._rpc(
+            "nucleo_flow_state",
+            {"execution_id": str(execution_id or "").strip()},
+            "estado do fluxo durável",
+        )
+
+    def flow_finish_ai(
+        self,
+        execution_id: str,
+        suspension_id: str,
+        requester_phone: str,
+        outcome: str,
+    ) -> dict[str, Any]:
+        return self._rpc(
+            "nucleo_flow_finish_ai",
+            {
+                "execution_id": str(execution_id or "").strip(),
+                "suspension_id": str(suspension_id or "").strip(),
+                "requester_phone": str(requester_phone or "").strip(),
+                "outcome": str(outcome or "").strip(),
+            },
+            "retomada do fluxo após IA",
+        )
+
+    def flow_cancel(self, execution_id: str, reason: str) -> dict[str, Any]:
+        return self._rpc(
+            "nucleo_flow_cancel",
+            {
+                "execution_id": str(execution_id or "").strip(),
+                "cancellation_reason": str(reason or "").strip(),
+            },
+            "cancelamento do fluxo durável",
+        )
+
     def _rpc(self, name: str, body: dict[str, Any], label: str) -> dict[str, Any]:
         credential = self._load()
         if credential.expires_at <= int(time.time()) + 60:
diff --git a/whatsapp-assistant/server.py b/whatsapp-assistant/server.py
index fe5419d..fc515e4 100644
--- a/whatsapp-assistant/server.py
+++ b/whatsapp-assistant/server.py
@@ -760,7 +760,12 @@ class InboundHandler(BaseHTTPRequestHandler):
                 and not operator_turn
                 and not chat_jid.endswith("@g.us")
             ):
-                result = self.chatbot_handler(payload)
+                chatbot_payload = dict(payload)
+                chatbot_payload["conversation_session_id"] = (
+                    decisao.sessao.id if decisao.sessao is not None else ""
+                )
+                chatbot_payload["conversation_epoch"] = 0
+                result = self.chatbot_handler(chatbot_payload)
                 next_owner = getattr(result, "next_owner", None)
                 handled = bool(getattr(result, "handled", False))
                 reason = str(getattr(result, "reason", "") or "")
diff --git a/whatsapp-assistant/main.py b/whatsapp-assistant/main.py
index a262d05..5d74fc6 100644
--- a/whatsapp-assistant/main.py
+++ b/whatsapp-assistant/main.py
@@ -97,6 +97,7 @@ def main() -> int:
             cfg.api_base_url,
             token,
         )
+        chatbot_executor.start()
     heartbeat = None
     if cfg.runtime_heartbeat_enabled and verifier is not None:
         heartbeat = runtime_heartbeat.RuntimeHeartbeat(
@@ -206,6 +207,7 @@ def main() -> int:
         heartbeat,
         command_poller,
         conversation_syncer,
+        chatbot_executor,
     )
 
 
@@ -218,6 +220,7 @@ def _wait_forever(
     heartbeat=None,
     command_poller=None,
     conversation_syncer=None,
+    chatbot_executor=None,
 ) -> int:
     done = threading.Event()
 
@@ -237,6 +240,8 @@ def _wait_forever(
         command_poller.stop()
     if heartbeat is not None:
         heartbeat.stop()
+    if chatbot_executor is not None:
+        chatbot_executor.stop()
     if notification_worker is not None:
         notification_worker.stop()
     worker.stop()
diff --git a/whatsapp-assistant/worker.py b/whatsapp-assistant/worker.py
index 25ee392..334be7c 100644
--- a/whatsapp-assistant/worker.py
+++ b/whatsapp-assistant/worker.py
@@ -235,6 +235,48 @@ class Worker:
             "contractVersion": self.cfg.runtime_contract_version,
         }
 
+    def _flow_suspension(self, recipient: str, disabled: bool, run_id: str) -> Optional[dict]:
+        if (
+            disabled
+            or self._operator_verifier is None
+            or not hasattr(self._operator_verifier, "flow_current")
+        ):
+            return None
+        try:
+            payload = self._operator_verifier.flow_current(recipient)
+        except OperatorVerificationError as exc:
+            audit.log("flow.current_failed", run_id=run_id, error=str(exc))
+            return None
+        if payload.get("status") != "suspended":
+            return None
+        if not payload.get("executionId") or not payload.get("suspensionId"):
+            audit.log("flow.current_invalid", run_id=run_id)
+            return None
+        return payload
+
+    def _finish_flow_ai(self, flow: dict, recipient: str, outcome: str, run_id: str) -> bool:
+        if self._operator_verifier is None or not hasattr(
+            self._operator_verifier, "flow_finish_ai"
+        ):
+            return False
+        try:
+            result = self._operator_verifier.flow_finish_ai(
+                str(flow.get("executionId") or ""),
+                str(flow.get("suspensionId") or ""),
+                recipient,
+                outcome,
+            )
+        except OperatorVerificationError as exc:
+            audit.log("flow.finish_ai_failed", run_id=run_id, outcome=outcome, error=str(exc))
+            return False
+        audit.log(
+            "flow.finish_ai",
+            run_id=run_id,
+            outcome=outcome,
+            status=result.get("status"),
+        )
+        return True
+
     def _set_readiness(self, connection_id: str, *, status: str, read=None, write=None) -> None:
         if not connection_id:
             return
@@ -587,6 +629,11 @@ class Worker:
         )
 
         token = load_api_token()
+        flow_suspension = self._flow_suspension(
+            recipient,
+            is_group or is_operator_turn,
+            run_id,
+        )
 
         # A posse de telefone é uma operação determinística. Não deixamos a
         # confirmação depender do Claude reconhecer o código e escolher a
@@ -660,6 +707,11 @@ class Worker:
             and bool(batch[0].organization_id or batch[0].connection_id)
             and not is_group
         ):
+            if flow_suspension and self._finish_flow_ai(
+                flow_suspension, recipient, "falha", run_id
+            ):
+                self.store.mark_completed(ids)
+                return
             notice = (
                 INTELLIGENCE_UNAVAILABLE_INTERNAL_NOTICE
                 if is_operator_turn
@@ -791,6 +843,11 @@ class Worker:
                 is_operator=is_operator_turn,
                 is_greeting=bool(SIMPLE_GREETING_RE.fullmatch(incoming_text)),
             )
+            if flow_suspension and self._finish_flow_ai(
+                flow_suspension, recipient, "falha", run_id
+            ):
+                self.store.mark_completed(ids)
+                return
             if (
                 is_customer_turn
                 and not is_group
@@ -872,6 +929,9 @@ class Worker:
             self._fail(ids, run_id, "send", str(e), started, exit_code=result.exit_code)
             return
 
+        if flow_suspension:
+            self._finish_flow_ai(flow_suspension, recipient, "sucesso", run_id)
+
         # So agora. Marcar antes transformaria falha de envio em mensagem
         # perdida em silencio.
         self.store.mark_completed(ids)
diff --git a/whatsapp-assistant/test_chatbot_runtime.py b/whatsapp-assistant/test_chatbot_runtime.py
index 2f2df95..3039a05 100644
--- a/whatsapp-assistant/test_chatbot_runtime.py
+++ b/whatsapp-assistant/test_chatbot_runtime.py
@@ -1,9 +1,127 @@
 import unittest
+from unittest.mock import patch
 
-from chatbot_runtime import _bot_matches, _plan, _render
+from chatbot_runtime import ChatbotExecutor, _bot_matches, _expression_matches, _plan, _render
+
+
+class FakeArbitro:
+    def __init__(self):
+        self.owners = []
+        self.closed = []
+
+    def definir_dono(self, connection_id, requester, owner, reason):
+        self.owners.append((connection_id, requester, owner, reason))
+
+    def fechar(self, connection_id, requester, reason):
+        self.closed.append((connection_id, requester, reason))
+
+
+class DurableVerifier:
+    def __init__(self):
+        self.claims = []
+        self.acks = []
+        self.started = []
+        self.finished = []
+
+    def chatbot_context(self, _sender):
+        return {
+            "schemaVersion": "chatbot-runtime-1",
+            "contact": {"name": "MARIA", "eventTypes": ["contact.created"]},
+            "chatbots": [{
+                "id": "11111111-1111-4111-8111-111111111111",
+                "version": 3,
+                "definition": {
+                    "condicoes": [{"tipo": "primeira_conversa"}],
+                    "canvas": {"versao": 3},
+                },
+            }],
+        }
+
+    def flow_start(self, *args):
+        self.started.append(args)
+        return {
+            "executionId": "execution",
+            "connectionId": "connection",
+            "requesterPhone": "5511999999999",
+            "revision": 0,
+            "status": "ready",
+        }
+
+    def flow_claim(self, _execution_id, _revision):
+        return self.claims.pop(0)
+
+    def flow_ack(self, execution_id, claim_token, selected_output, effect_status="confirmed"):
+        self.acks.append((execution_id, claim_token, selected_output, effect_status))
+        if selected_output == "padrao":
+            return {"executionId": "execution", "revision": 2, "status": "ready"}
+        return {"executionId": "execution", "revision": 4, "status": "suspended"}
+
+    def flow_pending(self):
+        return {"executions": []}
+
+    def flow_finish_ai(self, *args):
+        self.finished.append(args)
+        return {"status": "ready"}
 
 
 class ChatbotRuntimeTests(unittest.TestCase):
+    def test_nested_expression_fails_closed_when_any_item_is_invalid(self):
+        expression = {
+            "operador": "e",
+            "itens": [{"tipo": "tem_etiqueta", "etiquetaId": "tag-a"}, "invalid"],
+        }
+        self.assertFalse(_expression_matches(expression, {"tags": ["tag-a"]}, 0))
+
+    @patch("chatbot_runtime.send.send_message")
+    def test_canvas_v3_sends_message_then_suspends_for_ai(self, send_message):
+        verifier = DurableVerifier()
+        verifier.claims = [
+            {
+                "executionId": "execution", "revision": 1, "status": "executing",
+                "claimToken": "claim-message", "requesterPhone": "5511999999999",
+                "step": {"tipo": "enviar_mensagem", "texto": "Oi {nome}"},
+                "contact": {"name": "MARIA"},
+            },
+            {
+                "executionId": "execution", "revision": 3, "status": "executing",
+                "claimToken": "claim-ai", "requesterPhone": "5511999999999",
+                "step": {"tipo": "transferir", "destino": "ia"},
+                "contact": {"name": "MARIA"},
+            },
+        ]
+        executor = ChatbotExecutor(verifier, FakeArbitro(), "http://bridge", "token")
+
+        result = executor.handle({
+            "sender_phone": "5511999999999",
+            "message_id": "message-1",
+            "connection_id": "connection",
+            "conversation_session_id": "22222222-2222-4222-8222-222222222222",
+            "conversation_epoch": 0,
+        })
+
+        self.assertFalse(result.handled)
+        self.assertEqual(result.reason, "flow_waiting_ai")
+        send_message.assert_called_once_with("http://bridge", "token", "5511999999999", "Oi Maria")
+        self.assertEqual(verifier.started[0][1], "message-1")
+        self.assertEqual(verifier.acks[0][2:], ("padrao", "confirmed"))
+        self.assertEqual(verifier.acks[1][2:], ("", "confirmed"))
+
+    @patch("chatbot_runtime.time.time", return_value=200)
+    def test_poller_expires_stale_ai_suspension(self, _time):
+        verifier = DurableVerifier()
+        verifier.flow_pending = lambda: {"executions": [{
+            "executionId": "execution", "status": "suspended",
+            "suspensionId": "suspension", "suspensionExpiresAt": "1970-01-01T00:01:00Z",
+            "requesterPhone": "5511999999999",
+        }]}
+        executor = ChatbotExecutor(verifier, FakeArbitro(), "http://bridge", "token")
+
+        executor.poll_once()
+
+        self.assertEqual(
+            verifier.finished,
+            [("execution", "suspension", "5511999999999", "expired")],
+        )
     def test_matches_first_conversation_and_builds_message_then_transfer(self):
         contact = {
             "name": "MARIA SILVA",
diff --git a/whatsapp-assistant/test_operator_verification.py b/whatsapp-assistant/test_operator_verification.py
index 08a48f0..77a1886 100644
--- a/whatsapp-assistant/test_operator_verification.py
+++ b/whatsapp-assistant/test_operator_verification.py
@@ -158,6 +158,32 @@ class OperatorVerificationTests(unittest.TestCase):
             self.assertTrue(url.endswith("/rest/v1/rpc/nucleo_assistant_capabilities"))
             self.assertEqual(kwargs["json"]["expected_contract_version"], "fase-g-1")
 
+    def test_flow_rpc_methods_use_the_narrow_contract(self):
+        with tempfile.TemporaryDirectory() as directory:
+            path = Path(directory) / "robot.json"
+            credential(path)
+            session = FakeSession([FakeResponse(200, {"status": "ready"})])
+
+            OperatorVerifier(path, session=session).flow_start(
+                "5511999999999",
+                "message-1",
+                "11111111-1111-4111-8111-111111111111",
+                3,
+                "22222222-2222-4222-8222-222222222222",
+                4,
+            )
+
+            url, kwargs = session.calls[0]
+            self.assertTrue(url.endswith("/rest/v1/rpc/nucleo_flow_start"))
+            self.assertEqual(kwargs["json"], {
+                "requester_phone": "5511999999999",
+                "external_message": "message-1",
+                "selected_chatbot": "11111111-1111-4111-8111-111111111111",
+                "expected_version": 3,
+                "conversation_session": "22222222-2222-4222-8222-222222222222",
+                "conversation_epoch": 4,
+            })
+
     def test_intelligence_context_uses_v2_without_accepting_organization_ids(self):
         with tempfile.TemporaryDirectory() as directory:
             path = Path(directory) / "robot.json"
